#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

if [[ -f "${ROOT_DIR}/dist/cli.js" ]]; then
  exec node "${ROOT_DIR}/dist/cli.js" "$@"
fi

exec node --import tsx "${ROOT_DIR}/src/cli.ts" "$@"
